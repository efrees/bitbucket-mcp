node_modules/
dist/
*.log
.DS_Store

# Claude Code local settings — per-user, not shared
.claude/settings.local.json

# Claude Code worktrees — ephemeral, per-task working copies
.claude/worktrees/

# Local config & secrets — never commit
.env
.env.*
!.env.example
config.local.json
tokens.json
tokens.json.enc
